This intentionally empty directory prevents Vite from loading repository `.env`
files. Supply `VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY`, and
`VITE_CLOUD_API_URL` through the build-process environment.
